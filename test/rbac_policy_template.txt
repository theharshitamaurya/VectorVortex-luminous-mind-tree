RBAC Policy Template
Sections: Purpose & Scope, Role Definitions (DB_APP_READ → read, DB_APP_READWRITE → readWrite, DB_ANALYST → read on replica, DB_ADMIN → dbAdmin+readWrite, DB_OPS → clusterMonitor, DB_SUPERUSER → root/break-glass), Access Request Process (ticket → manager approval → security review → DBA provisions → 90-day review cycle), Least Privilege rules (no clusterAdmin for apps, no shared credentials, no credentials in source code), Credential Management table (rotation frequencies, storage methods per role type), Audit & Compliance (quarterly access reviews for prod, monthly for privileged roles, 12-month log retention, SIEM forwarding within 5 minutes), Violations section, and an annual policy review schedule.