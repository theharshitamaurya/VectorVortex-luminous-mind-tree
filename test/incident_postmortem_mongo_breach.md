Incident Post-Mortem: MongoDB Security Breach
Header: Incident ID, severity (P1), date, status (closed), confidentiality notice. Sections: Executive Summary (brief: what happened, how long, data scope), Timeline table (provisioning error → first unauthorized access → SIEM alert → containment → remediation → all-clear), Root Cause Analysis (misconfigured replica set node provisioned without auth; no automated compliance check caught it), Impact Assessment (number of records exposed, data types, affected services), Immediate Remediation Actions (node isolated, auth enabled cluster-wide, credentials rotated, firewall rules tightened), Long-Term Corrective Actions (IaC policy-as-code checks, automated CIS benchmark scanning, pre-production compliance gate in CI/CD), Lessons Learned (what went well, what failed, what was lucky), Action Items table (owner, due date, status), and a blameless culture statement.
